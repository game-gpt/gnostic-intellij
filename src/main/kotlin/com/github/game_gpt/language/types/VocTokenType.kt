package com.github.game_gpt.language.types

import com.github.game_gpt.language.GnosticWidgetLanguage
import com.intellij.psi.tree.IElementType

class VocTokenType(debugName: String) : IElementType(debugName, GnosticWidgetLanguage)
